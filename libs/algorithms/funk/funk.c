#include "funk.h"
#include <stdio.h>
void plosh(int a, int b)
{
    int plosh = 0;
    plosh = a * b;
    printf("%d \n", plosh);
}
void per(int a, int b)
{
    int per = 0;
    per = 2 * (a + b);
    printf("%d \n", per);
}